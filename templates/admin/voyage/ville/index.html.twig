{% extends 'base_admin.html.twig' %}

{% block title %}Ville index{% endblock %}

{% block body %}
    <div class="row">
        <div class="col-sm-12 col-md-12 ">
            <div class="panel panel-bd lobidrag">
                <div class="panel-heading">
                    <div class="panel-title">
                        <h4>
                            <a href="{{ path('voyages_ville_new') }}" class="btn btn-sm btn-info"
                               title="List"><i class="fa fa-list"></i> Ajouter</a>
                            {#                            <?php if($fleet_type->id): ?>#}
                            {#                            <a href="<?php echo base_url('fleet/fleet_type/form') ?>" class="btn btn-sm btn-info"#}
                            {#                               title="Add"><i class="fa fa-plus"></i> <?php echo display('add') ?></a>#}
                            {#                            <?php endif; ?>#}
                        </h4>
                    </div>
                </div>
                <div class="panel-body">

                    <table class="datatable table table-bordered table-hover">
                        <thead>
                        <tr>

                            <th>Ville</th>
                            <th>Description</th>
                            <th>Status</th>
                            <th>actions</th>
                        </tr>
                        </thead>
                        <tbody>
                        {% for ville in villes %}
                            <tr>

                                <td>{{ ville.NomVille }}</td>
                                <td>{{ ville.Description }}</td>
                                <td>{{ ville.Status ? 'Active' : 'Inactive' }}</td>
                                <td>
                                    {{ include('admin/voyage/ville/_delete_form.html.twig') }}
                                    <a href="{{ path('voyages_ville_edit', {'id': ville.id}) }}" class="btn btn-info btn-sm" data-toggle="tooltip" data-placement="left" title="Update"><i class="fa fa-pencil" aria-hidden="true"></i></a>

                                </td>
                            </tr>
                        {% else %}
                            <tr>
                                <td colspan="5">no records found</td>
                            </tr>
                        {% endfor %}
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>


{#    <a href="{{ path('ville_new') }}">Create new</a>#}
{% endblock %}
