{% extends 'base.html.twig' %}

{% block title %}Lignes{% endblock %}

{% block body %}
    <h1>Lignes</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ligne.id }}</td>
            </tr>
            <tr>
                <th>NomLigne</th>
                <td>{{ ligne.NomLigne }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ ligne.Status ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('lignes_index') }}">back to list</a>

    <a href="{{ path('lignes_edit', {'id': ligne.id}) }}">edit</a>

    {{ include('lignes/_delete_form.html.twig') }}
{% endblock %}
