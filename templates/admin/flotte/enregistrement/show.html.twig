{% extends 'base.html.twig' %}

{% block title %}EnregistrementVehicule{% endblock %}

{% block body %}
    <h1>EnregistrementVehicule</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ enregistrement_vehicule.id }}</td>
            </tr>
            <tr>
                <th>NumeroVehicule</th>
                <td>{{ enregistrement_vehicule.NumeroVehicule }}</td>
            </tr>
            <tr>
                <th>NumeroMoteur</th>
                <td>{{ enregistrement_vehicule.NumeroMoteur }}</td>
            </tr>
            <tr>
                <th>NumeroChassis</th>
                <td>{{ enregistrement_vehicule.NumeroChassis }}</td>
            </tr>
            <tr>
                <th>Marque</th>
                <td>{{ enregistrement_vehicule.Marque }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ enregistrement_vehicule.status ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>Model</th>
                <td>{{ enregistrement_vehicule.Model }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('enregistrement_vehicule_index') }}">back to list</a>

    <a href="{{ path('enregistrement_vehicule_edit', {'id': enregistrement_vehicule.id}) }}">edit</a>

    {{ include('enregistrement_vehicule/_delete_form.html.twig') }}
{% endblock %}
