<form method="post" action="{{ path('flotte_enregistrement_vehicule_delete', {'id': enregistrement_vehicule.id}) }}"
      onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ enregistrement_vehicule.id) }}">
    <button class="btn btn-danger btn-sm" data-toggle="tooltip" data-placement="right" title="Delete "><i
                class="fa fa-trash-o" aria-hidden="true"></i></button>
</form>
